(DEFUN PRINTNIV(LST)
((NULL LST))
(SETQ AUX (CAR LST))
(IF (ATOM AUX) (PRINT AUX) (SETQ LST (APPEND LST AUX)))
(PRINTNIV (CDR LST))
)






; )LOAD {printlev.txt=

; )PRINTNIV LS=

; )IF )ATOM )CAR LST== )PRINT )CAR LST== )SETQ LST )APPEND LST )CAR LST====

; )SETQ LST )CDR LST==