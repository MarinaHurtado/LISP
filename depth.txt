(DEFUN DEPTH (LST)
(SETQ MAX 0)
(DD LST 0)
(PRINT MAX)
)



(DEFUN DD (LST ACT)
((NULL LST) ((> ACT MAX) (SETQ MAX ACT)))

(SETQ AUX (CAR LST))
(IF (LISTP AUX) (DD AUX (+ 1 ACT)) NIL )

(DD (CDR LST) ACT )

)


; )LOAD {depth.txt=