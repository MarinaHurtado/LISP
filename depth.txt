(DEFUN DEPTH (LST ACT MAX)
((NULL LST) ((> ACT MAX) (SETQ MAX ACT)))

(IF (AND (ATOM(CAR LST))(NOT (NULL (CDR LST)))(LISTP (CADR LST))) 
(DEPTH (CADR LST) (INCQ ACT)  MAX) 
NIL)

(DEPTH (CDR LST) ACT MAX)
)