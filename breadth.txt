(DEFUN BREADTH(LST)
(SETQ MAX 0)
(BREADTH1 LST 0)
)

(DEFUN BREADTH1 (LST ACT)
((NULL LST)((> ACT MAX) (SETQ MAX ACT)))
(SETQ AUX (CAR LST))
(IF (ATOM AUX) (INCQ ACT)
(BREADTH1 AUX 0))
(BREADTH1 (CDR LST) ACT)
)

; Hay que mandarla llamar (BREADTH LST 0 0)